{
  "name": "seo_blog_root",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "install:all": "npm --prefix client install --no-fund && npm --prefix server install --no-fund",
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm --prefix server start\" \"npm --prefix client run dev\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}

